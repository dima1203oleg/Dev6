import React from "react";
import UnavailableSurface from "./UnavailableSurface";
export default function LiveAnalyticalCore(_props: Record<string, unknown>) {
  return <UnavailableSurface title="Живе аналітичне ядро" source="потоку аналітичних подій" />;
}
